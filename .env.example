# Telegram bot token (required for Telegram mode)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Auth: Claude Max subscription via Claude CLI OAuth (automatic)
# Credentials are read from macOS Keychain or ~/.claude/.credentials.json
# No API key needed — just have Claude CLI signed in with a Max subscription.
